{
  "name": "mandalorian-translation",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "got": "^11.8.2",
    "https": "^1.0.0",
    "node-fetch": "^2.6.1",
    "superagent": "^6.1.0"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
